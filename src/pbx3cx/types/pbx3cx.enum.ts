export enum CallType {
  local = 'local',
  mobile = 'mobile',
  group = 'group',
}

export enum ForwardType {
  away = 'Away',
  available = 'Available',
  outOfOffice = 'Out of office',
  customOne = 'Custom 1',
  customTwo = 'Custom 2',
}
