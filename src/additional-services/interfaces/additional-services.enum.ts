export enum ServicesType {
  mail = 'mail',
  queue = 'queue',
  extension = 'extension',
}

export enum ExtensionForwardRuleType {
  mobile = 'mobile',
  extension = 'extension',
  external = 'external',
}
